apiVersion: source.toolkit.fluxcd.io/v1beta2
kind: GitRepository
metadata:
  name: mongo
  namespace: default 
spec:
  interval: 1m
  url: https://github.com/bitnami/charts/tree/master/bitnami/mongodb
  ref:
    branch: master
